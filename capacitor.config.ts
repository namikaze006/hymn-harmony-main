import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.himnario.bautista',
  appName: 'Himnario Bautista',
  webDir: 'dist'
};

export default config;
